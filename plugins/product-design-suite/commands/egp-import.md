---
description: Ingest existing PRD/SAD/ADR/SDD docs (plus a legacy SRS) into the suite via egp-import
argument-hint: [path to existing docs]
---
Use the egp-import skill to ingest existing product docs, map them to the templates, and write a gap report at `workspace/outputs/current/governance/import-gap-report.md` before authoring. $ARGUMENTS
