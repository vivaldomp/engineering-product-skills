{
  "name": "engineering-product-skills",
  "version": "0.1.1",
  "private": true,
  "description": "Tooling and tests for the product-design-suite Claude Code plugin.",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node tools/validate-plugin.js"
  }
}
